import { baseRoutes } from "../../../helpers/baseRoutes";

const accessRoute = {
  PLAN_SUBSCRIPTION: {
    path: `${baseRoutes.userBaseRoutes}plan-subscription`,
  },
};

export default accessRoute;
