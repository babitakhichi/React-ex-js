import config from "../../../config";

const Media = {
  profileImage: `${config.BASE_URL}/v2.0/admin/uploadUserImage`,
  uploadTeamImage :`${config.BASE_URL}/v2.0/uploadTeamImage`,
  uploadClientImage :`${config.BASE_URL}/v2.0/uploadClientImage`
};

export default Media;
