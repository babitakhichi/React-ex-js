import config from "../../../config";

const Media = {
  profileImage: `${config.BASE_URL}/v2.0/uploadUserImage`,
  documentImage: `${config.BASE_URL}/v2.0/document-upload`,
};

export default Media;
